[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "what-to-id"
version = "0.1.0"
description = "Tests whether the order of the iNaturalist identification queue changes what gets identified: randomised lists, exposure manifest, read-back"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Wietze Suijker", email = "17180130+wietzesuijker@users.noreply.github.com" }]
dependencies = [
    "numpy>=1.26",
    "pandas>=2.2",
    "pyarrow>=15",
    "requests>=2.31",
    "scipy>=1.11",
]

[project.scripts]
what-to-id = "what_to_id.cli:main"

[project.optional-dependencies]
# labelfirst is private and not on PyPI; pinned to the immutable 0.9.0 commit, same pin as
# speciesfirst. Only the similarity arm's seed picker and the separability report need it.
similarity = [
    "labelfirst @ git+https://github.com/traitlab/labelfirst.git@5fed14e1870fb8e6ad390d60a9b12e60eaa549f2",
]
embed = ["torch>=2.1", "torchvision>=0.16", "open_clip_torch>=2.24", "pillow>=10.0"]
dev = ["pytest>=7.4", "ruff>=0.4"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/what_to_id"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers -m 'not network'"
markers = ["network: hits the live iNaturalist API"]
